__pycache__/
*.pyc
.venv/
.DS_Store
dist/
.coverage
coverage.xml
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Acquired source files and the acquisition manifest. Never in git, never in CI:
# these are CAL FIRE's files, re-downloadable from the endpoints in PROVENANCE.md.
data/raw/
build/

# Page-check toolchain. package.json and package-lock.json are in git; the tree is not.
node_modules/

# Playwright run output. The specs and their lockfile are in git; the results are not.
tools/a11y_browser/test-results/
tools/a11y_browser/playwright-report/
